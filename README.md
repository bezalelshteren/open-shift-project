               ##openshift project


               the project pulls a image from a registry in a deployment.yaml file thet use secrate.yaml to protect the password and aPVC
               its contain elso a python program (server) thet get recuest from all computers (becoase its in open shift) 
               and send them the all table from the sql (i insert the data from inside the pod)
               
